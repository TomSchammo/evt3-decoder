
# EVT3-Decoder

This repository contains the code for a custom EVT3 decoder for CD Events, written in C++.
Non CD-Events such as `TRIGGER`, `CUSTOM` and `CONTINUE` are not supported as I don't have data to verify those.

TOOD: Added extra data types too and do benchmark
